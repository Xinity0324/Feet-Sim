local ringNumbers = {9, 15, 18, 8, 19, 6, 7}  -- Including ring number 6

-- Function to collect a ring with the given number
local function collectRing(number)
    local args = {
        [1] = "Collect Ring",
        [2] = tostring(number)
    }
    game:GetService("ReplicatedStorage"):WaitForChild("RemoteEvent"):FireServer(unpack(args))
end

-- Function to collect all rings simultaneously
local function collectAllRings()
    local coroutines = {}
    for _, number in ipairs(ringNumbers) do
        table.insert(coroutines, coroutine.create(function()
            collectRing(number)
        end))
    end
    for _, co in ipairs(coroutines) do
        coroutine.resume(co)
    end
end

-- Loop to collect all rings every 10.1 seconds
while true do
    collectAllRings()
    wait(10.1)
end